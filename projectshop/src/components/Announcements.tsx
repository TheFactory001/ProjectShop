import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  background: #f5f5f5;
  height: 40px;
`;
const Announcements = () => {
  return <Wrapper>Announcements</Wrapper>;
};

export default Announcements;
